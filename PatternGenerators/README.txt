This folder is for Pattern Generators that need to be run once only. Patterns (like Random) that generate nondeterministic spec files are handled differently.

Run ./GenerateAll.sh to generate all the patterns in the folder.